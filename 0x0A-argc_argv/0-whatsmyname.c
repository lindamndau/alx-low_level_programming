#include <stdio.h>
#include <main.h>
/**
 * This main program prints the its own name
 *
 * Returns exit  success
 */
int main (int argc, char ** argv)
{
	if(argc--)
	{
		printf("%d\n", argc[0];
		return (0);
	}
}
