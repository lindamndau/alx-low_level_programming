#include "search_algos.h"
/**
 * @int interpolation_search - function searches for a value in a sorted array of integers using interprolation serch algorithm
 * Returns index where value is sorted
 */
int interpolation_search(int *array, size_t size, int value)
{
	size_t i = 0;
